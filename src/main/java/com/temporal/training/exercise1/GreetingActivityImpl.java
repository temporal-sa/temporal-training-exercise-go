package com.temporal.training.exercise1;

/**
 * Activity implementation contains the actual business logic.
 * This is where you would make external API calls, database operations, etc.
 */
public class GreetingActivityImpl implements GreetingActivity {
    
    @Override
    public String createGreeting(String name) {
        // TODO: Return a greeting message in the format "Hello, [name]!"
        return null; // Replace with actual greeting
    }
}